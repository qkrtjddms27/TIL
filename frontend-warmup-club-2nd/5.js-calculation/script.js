const num1 = 20;
const num2 = 10;

let val;
val = num1 + num2;
val = num1 * num2;
val = num1 - num2;
val = num1 / num2;
val = num1 % num2;

val = Math.E;
val = Math.PI;
val = Math.LN10;

// ... 등등 많은 함수가 있음

Math.round(2.4); // 가장 가까운 숫자를 리턴한다.

